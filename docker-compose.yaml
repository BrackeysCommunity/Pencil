version: '3.9'
services:
  pencil:
    container_name: Pencil
    build: .
    volumes:
      - type: bind
        source: ./logs
        target: /app/logs
      - type: bind
        source: ./data
        target: /app/data
    restart: always
    env_file:
      - .env
